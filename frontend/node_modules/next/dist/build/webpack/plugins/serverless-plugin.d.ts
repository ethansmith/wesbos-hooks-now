import { Compiler } from 'webpack';
export declare class ServerlessPlugin {
    private buildId;
    private isServer;
    private isTrace;
    private isFlyingShuttle;
    constructor(buildId: string, { isServer, isTrace, isFlyingShuttle, }: {
        isServer: boolean;
        isTrace: boolean;
        isFlyingShuttle: boolean;
    });
    apply(compiler: Compiler): void;
}
