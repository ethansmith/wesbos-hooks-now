// This is client side config only - don't put anything in here that shouldn't be public!
export const LOCAL_BACKEND_URL = `http://localhost:4444`;
export const PROD_BACKEND_URL = `https://provision-backend.ethansmith.now.sh`;
export const perPage = 4;
