#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@kbrandwijk/swagger-to-graphql/bin/swagger2graphql" "$@"
  ret=$?
else 
  node  "$basedir/../@kbrandwijk/swagger-to-graphql/bin/swagger2graphql" "$@"
  ret=$?
fi
exit $ret
